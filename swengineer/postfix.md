### 트리의 순회

## 전위

- a b 연산자를 먼저 읽고 피연산자 처리

## 중위

a + b 사람 읽기 편한 방식

## 후위

a b + 연산자가 피연산자 뒤

(a + b ) _ c
전위 _ + a b c
후위 a b + c \*

### 연산자

## 전위

루트 -> 왼쪽 -> 오른쪽

## 중위

왼쪽 -> 루트 -> 오른쪽

## 후위

왼쪽 오른쪽 루트

biba 모델 - 무결성보장 no read down, no write up
만리장성모델 - 사용자 이전 접근에 따라 권한 실시간 변경
벨-라파듈라 모델 - no read up no write down 기밀성 강조 군대식
클라크 윌슨 - 무결성 - 상업적 환경 - 특정 소프트웨어를 통해 데이터 접근 가능
